package systemFunctions;

public interface MenuAction {
    public void execute();
    //aqui dentro vai cada recho de codigo das funcionalidades que seriam executadas no menu da main
    //ex: login(), cadastroAluno(), abrirTicket(), etc
}
